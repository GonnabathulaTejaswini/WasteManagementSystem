package com.klu.route.repository;

import com.klu.route.entity.WasteRoute;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WasteRouteRepository
        extends JpaRepository<WasteRoute, Long> {
}